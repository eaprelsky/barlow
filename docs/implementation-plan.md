# Реализация ревизии от 7 сентября 2026

Основание: `D:\YandexDisk\cloudwork\personal\deepresearch\2026-09-07_barlow`.
Пользователь поручил реализовать весь план. Аудит остаётся исходным срезом;
статус выполнения ведётся здесь. Аудит не превращает условные рекомендации
в обещание измеренного покрытия 90% или готовности 300 непроверенных тембров.

## Волны и критерии

- [x] 0. Контракт голоса: A01–A07, независимые velocity/duration,
  унисон/хвосты, амплитуда гармоник, FM DAG, постоянная синхронизация,
  rootHz с явным включением key tracking; meaningful DSP regression tests.
- [x] 0b. Границы данных: A12, A13, A21 — Tauri filenames, импорт,
  версии/ссылки/лимиты до побочных эффектов.
- [x] 1. Домен и доступность: parameter registry, commands/history,
  Knob/NumField/Dialog, gain ownership, autosave и ошибки сохранения.
- [ ] 2. Основа палитры: sampler, tags/macros/preview, около 120
  проверенных пресетов со стабильными ID и музыкальными брифами.
- [ ] 3. IDM performance: слои, slices/choke, locks/ratchets/microtiming,
  portamento, MSEG; общий event plan и live/offline parity.
- [ ] 4. DSP: wavetable/VA, ring/fold/comb, voice budget и диагностика;
  aliasing/stress QA, обоснованное расширение примерно до 180.
- [ ] 5. Библиотека 300: packs, курация, проверка поиска и брифов;
  modal/spectral — по подтверждённым пробелам, а не ради количества.
- [ ] Сквозные пункты: A09–A11, A14–A20, A22 и реестр U001–U219.

Каждая волна обновляет тематическую спецификацию и проходит build, lint,
релевантные DSP/UI-тесты, golden и smoke. Изменение golden допустимо только
с объяснённым изменением звука. После законченной волны — desktop и deploy-web.
Ошибки публикации фиксируются отдельно от успешности реализации.

## Принятые решения

- Сохраняем JSON, Hz, независимые циклы и Web Audio backend.
- Новые поля с безопасными дефолтами; старые сэмплы сохраняют ratio playback.
- Исправления подтверждённых дефектов звука меняют результат старых патчей;
  этот переход документируется явно, без заявления о побитовой совместимости.
- Лицензируемый сторонний аудиоконтент и внешние платные AI-запросы не нужны
  для исправлений и инфраструктуры; каталог не подменяем пустыми пресетами.

## Текущий статус

Волны 0/0b реализованы: schema v40, 45/45 DSP+import проверок, два
Rust-теста файловых имён. Build и smoke проходят. Golden обновлён после
объяснённого изменения амплитуды/огибающих; повторная сверка проходит.
Обычный cargo test без ограничения параллелизма упёрся в ресурсы Windows;
изолированные Rust-тесты прошли. Сборка desktop использует один job.
Волна 0 опубликована в вебе и установлена в desktop (49dbad9).
Волна 1 реализована: schema v41, скалярный parameter registry, явная
история жестов, доступные общие контролы и диалоги, клавиатура стана,
поиск Ctrl/Cmd+K, дробный rate, относительный уровень партии и autosave.
History/parameter и UI-контракты проходят, DSP+import — 48/48; golden
с прежним эталоном v40 проходит. Полный U001–U219 остаётся сквозным списком,
эта отметка не означает готовность каждой будущей UX-рекомендации.
Волна 1 опубликована в вебе и desktop (8ab88a5).
Волна 2a: schema v42, 120 звучащих пресетов + два шаблона, tags/stable IDs,
макросы с назначениями, реверс/петля/crossfade прямого сэмплера, исправлены
сохранение и preview пресетов. Bank QA 120/120, DSP+import 56/56, UI и
golden проходят. См. palette.md. Это не закрывает мультисэмпловые зоны,
профессиональную курацию и весь этап 2. Волны 3–5 ещё не выполнены.
Seed fixtures не означают готовый A16.

Волна 2a опубликована в вебе и desktop (f0d5f80).
Волна 2b: schema v43, частотные/velocity зоны, общий сборщик ассетов,
A20 — очередь/abort/late result, защищённый старт транспорта. DSP 61/61,
банк 120/120, UI/jobs/golden проходят; опубликована в вебе и desktop (1ea7cf6).

Волна 3a реализована: schema v44, ratchet/microtiming, независимые seed-потоки,
общий event plan, очередь будущих атак, порядок mono, исправление темпа
и последнего окна на переходе сцены, ограниченный план WAV с отдельными
цепочками повторных появлений сцены, voice budget и видимая диагностика.
Контракт и незакрытые границы — performance.md. Это частичная реализация
A09/A11/A16; полный live/offline parity и A10 ещё не закрыты.
Проверки: DSP/import/planning 83/83, build, lint (без ошибок), UI,
smoke/bridge проходят. Golden обновлён после изолированной проверки
изменения старта LFO, повторная сверка проходит. Desktop/web опубликованы (d3df1e7).

Волна A14 реализована: явное подключение локального агента, взаимный
challenge/HMAC, session code, origin allowlist, read/write/transport,
bounded frames/handshake/commands, защита JSON pointer и валидация
получившегося патча. WebSocket и браузерные негативные тесты, build,
lint (без ошибок), UI, smoke/bridge и golden проходят. Desktop/web опубликованы (288b49c).

Волна A17 реализована: schema v45, стабильные FX ID и адресные кривые/моды,
единый dry/wet, ограничение модулированного feedback/time, владелец параметра
в live, выбор эффекта/клавиатурная перестановка, доступность кривой и undo
целого жеста. DSP 91/91, банк 120/120, прежний golden, build, lint без ошибок,
UI/FX UI и smoke/bridge проходят; эффекты — effects.md. Desktop/web опубликованы (e610266).

Волна A10 реализована: явные natural/trim при экспорте, расчёт голосов и FX,
ограничение хвоста до выделения PCM, остановка master noise, удаление
служебного preroll и лишней тишины, окно длины/режима/оценки/результата.
Это политика окончания последней сцены; внутренние переходы сохраняют
сценические fade. Контракт — export.md. 14 экспортных PCM/плановых проверок
и браузерный WAV download проходят; DSP 91/91, прежний golden, build,
lint без ошибок, UI и smoke/bridge проходят. Desktop/web опубликованы (c4b80d4).

Волна A11b реализована: отдельный бюджет цепочек/IR/модуляторов, байтовые
кэши, preflight WAV и пропуск пустых партий, диагностика затронутых треков,
подготовка тяжёлого графа до начала транспорта. Resource/stress-контракты
(12 проверок графа и сценарий UI), build, lint без ошибок, DSP 91/91,
банк 120/120, export/UI/smoke/bridge и прежний golden проходят.
Подробнее и оставшиеся границы — performance.md. Публикация — следующий шаг.
